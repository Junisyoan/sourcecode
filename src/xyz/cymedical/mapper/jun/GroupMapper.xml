<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" 
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="xyz.cymedical.mapper.jun.GroupMapper">

<!-- 更新表信息 -->
	<update id="updateGroupInfo">
		update tb_group set pamount=#{pnum}, totalmoney=#{price} where file_id = #{fid}
	</update>
<!-- 查询团检表 -->
	<select id="queryGroupByFileId" parameterType="int" resultType="xyz.cymedical.entity.jun.Group">
		select * from tb_group where file_id = #{fid}
	</select>
<!-- 生成团检表 -->
	<insert id="insert">
		insert into tb_group(company_id,file_id)
		values(#{cid},#{fid})
	</insert>
</mapper>