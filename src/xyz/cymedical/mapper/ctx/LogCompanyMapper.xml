<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" 
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="xyz.cymedical.mapper.ctx.LogCompanyMapper">

	<select id="queryByName" parameterType="java.lang.Integer"
		resultMap="logcompanyMap">
		select * from tb_log_company where company_id =
		#{company_id}
	</select>

	<select id="selectCompanyid" parameterType="java.lang.String"
		resultType="xyz.cymedical.entity.jun.Company">
		select * from tb_company where name = #{name}
	</select>

	<resultMap type="xyz.cymedical.entity.ctx.LogCompany"
		id="logcompanyMap">
		<association property="company" column="name"
			javaType="xyz.cymedical.entity.jun.Company" select="selectCompanyid">
		</association>
	</resultMap>

</mapper>